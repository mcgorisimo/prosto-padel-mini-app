HOST=127.0.0.1
PORT=3000
CRM_PROVIDER=disabled
DATABASE_ENABLED=false
DATABASE_URL=

# Player profile photos remain disabled until an S3-compatible bucket and
# public HTTPS object base are configured. In deployed environments remove
# each direct credential key entirely and use its mounted-file alternative;
# direct and file forms together (including an empty direct value) are rejected.
PROFILE_PHOTO_STORAGE_ENABLED=false
PROFILE_PHOTO_STORAGE_ENDPOINT=
PROFILE_PHOTO_STORAGE_REGION=
PROFILE_PHOTO_STORAGE_BUCKET=
# Keep this public read URL configured after the first custom photo exists,
# even when PROFILE_PHOTO_STORAGE_ENABLED is later set to false.
PROFILE_PHOTO_PUBLIC_BASE_URL=
PROFILE_PHOTO_STORAGE_ACCESS_KEY_ID=
# PROFILE_PHOTO_STORAGE_ACCESS_KEY_ID_FILE=/run/secrets/profile_photo_access_key_id
PROFILE_PHOTO_STORAGE_SECRET_ACCESS_KEY=
# PROFILE_PHOTO_STORAGE_SECRET_ACCESS_KEY_FILE=/run/secrets/profile_photo_secret_access_key

# Alternative database component mode. Remove DATABASE_URL entirely before
# enabling these values; direct URL and component mode are mutually exclusive.
# DATABASE_HOST=postgres
# DATABASE_PORT=5432
# DATABASE_NAME=prosto_padel_test_migration_cycle
# DATABASE_USER=backend_auth_app
# DATABASE_PASSWORD_FILE=/run/secrets/backend_auth_app_password

# Telegram authentication remains disabled by default. Direct secret values
# are retained for local/test compatibility. For a mounted-file secret, remove
# the corresponding direct key entirely; setting both, even with an empty
# direct value, is rejected.
TELEGRAM_AUTH_ENABLED=false
TELEGRAM_BOT_TOKEN=
# TELEGRAM_BOT_TOKEN_FILE=/run/secrets/telegram_bot_token
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=
TELEGRAM_IDENTITY_LOOKUP_PEPPER_BASE64=
# TELEGRAM_IDENTITY_LOOKUP_PEPPER_BASE64_FILE=/run/secrets/telegram_lookup_pepper
TELEGRAM_LOGIN_WORKFLOW_HMAC_SECRET_BASE64=
# TELEGRAM_LOGIN_WORKFLOW_HMAC_SECRET_BASE64_FILE=/run/secrets/telegram_workflow_hmac
TELEGRAM_LOGIN_UUID_NAMESPACE=
