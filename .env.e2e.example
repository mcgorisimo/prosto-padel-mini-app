# Copy to .env.e2e.local after the backend-only staging fixture contract is
# approved. Do not commit .env.e2e.local or any credentials.

E2E_TARGET=staging
E2E_BACKEND_BASE_URL=https://staging-api.example.test
